[project]
name = "agent-data-duckdb"
version = "0.1.0"
description = "agent_data DuckDB extension for reading AI coding agent session data"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
